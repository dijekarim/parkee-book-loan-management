version: '3.8'

services:
  # PostgreSQL Database
  db:
    image: postgres:15
    environment:
      POSTGRES_PASSWORD: password
      POSTGRES_USER: postgres
      POSTGRES_DB: book_loan_development
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Rails Backend API
  backend:
    image: parkee-book-loan-management-backend:latest
    command: /bin/sh -c "bundle install && rm -f tmp/pids/server.pid && bundle exec rails s -b 0.0.0.0 -p 4000"
    volumes:
      - ./backend:/app
      - gem_cache:/usr/local/bundle
    ports:
      - "4000:4000"
    environment:
      - DB_HOST=db
      - DB_PORT=5432
      - DB_USERNAME=postgres
      - DB_PASSWORD=password
      - DB_NAME=book_loan_development
      - RAILS_ENV=development
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4000/up"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Vue.js Frontend (Development)
  frontend:
    image: parkee-book-loan-management-frontend:latest
    volumes:
      - ./frontend:/app
      - node_modules:/app/node_modules
    ports:
      - "5173:5173"
    environment:
      - NODE_ENV=development
      - VITE_API_BASE_URL=http://backend:4000
    depends_on:
      - backend
    command: npm run dev -- --host
    deploy:
      replicas: 2
      labels:
        - traefik.enable=true
        - traefik.http.routers.parkee_fe.rule=Host(`parkee.ikutfoto.id`)
        - traefik.http.routers.parkee_fe.entrypoints=websecure
        - traefik.http.routers.parkee_fe.tls.certresolver=letsencrypt
        - traefik.http.services.parkee_fe.loadbalancer.server.port=5173

volumes:
  postgres_data:
  gem_cache:
  node_modules:
